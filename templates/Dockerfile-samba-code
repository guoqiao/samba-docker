FROM samba-env

ENV PYTHONPATH="{{SAMBA_REPO_DIR}}/bin/python"

RUN git clone --depth 30 --branch {{SAMBA_REPO_BRANCH}} {{SAMBA_REPO_URL}} && cd samba && ./configure.developer && make -j && sudo make install

# add util scripts
ADD --chown=samba *.sh ./
RUN chmod a+x *.sh
